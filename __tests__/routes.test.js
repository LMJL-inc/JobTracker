const request = require('supertest');
const app = require('../server/server');

describe('route tests', () => {
   
        it('does testing work', () => {
            expect(2 + 2).toBe(4)
        })
  
})